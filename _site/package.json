{
  "devDependencies": {
    "autoprefixer": "latest",
    "cssnano": "latest",
    "postcss": "latest",
    "tailwindcss": "latest"
  },
  "scripts": {
    "build": "mkdir -p _site && npm run build"
  }
}
